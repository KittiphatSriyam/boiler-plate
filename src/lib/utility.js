export const isNotEmpty = (value) => {
  return value != "" && value != null && value != undefined;
};
export const isEmpty = (value) => {
  return value == "" || value == null || value == undefined;
};
